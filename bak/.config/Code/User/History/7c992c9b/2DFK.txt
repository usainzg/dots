get_property(dialect_libs GLOBAL PROPERTY MLIR_DIALECT_LIBS)
get_property(conversion_libs GLOBAL PROPERTY MLIR_CONVERSION_LIBS)

set (LIBS
    ${dialect_libs}
    ${conversion_libs}
    AffineDistributeToMPI
    MLIROptLib
    MLIRPass
    MLIRRegisterAllDialects
    MLIRRegisterAllExtensions
    MLIRRegisterAllPasses
)

# add_llvm_executable(topoly-distmem-opt topoly-distmem-opt.cpp)
add_mlir_tool(topoly-distmem-opt 

    topoly-distmem-opt.cpp

    DEPENDS
    ${LIBS}
)

llvm_update_compile_flags(topoly-distmem-opt)
target_link_libraries(topoly-distmem-opt PRIVATE ${LIBS})

mlir_check_all_link_libraries(topoly-distmem-opt)
