configure_lit_site_cfg(
    ${CMAKE_CURRENT_SOURCE_DIR}/lit.cmake.site.cfg.py.in
    ${CMAKE_CURRENT_BINARY_DIR}/lit.site.cfg.py
    MAIN_CONFIG
    ${CMAKE_CURRENT_SOURCE_DIR}/lit.cmake.cfg.py
)

set (TOPOLY_TEST_DEPENDS
    FileCheck count not
    topoly-opt
)

add_lit_testsuite(check-topoly "Running the MLIR topoly regression tests"
    ${CMAKE_CURRENT_BINARY_DIR}
    DEPENDS ${TOPOLY_TEST_DEPENDS}
)
set_target_properties(check-topoly PROPERTIES FOLDER "Tests")

add_lit_testsuites(TOPOLY ${CMAKE_CURRENT_SOURCE_DIR} DEPENDS ${TOPOLY_TEST_DEPENDS})